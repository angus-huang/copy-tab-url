{
  "manifest_version": 2,
  "name": "Copy Tab URL",
  "version": "1.0",

  "description": "Adds a tab context menu item to copy the tab's URL.",

  "permissions": [
    "menus",
    "tabs",
    "clipboardWrite"
  ],

  "background": {
    "scripts": ["copy-tab-url.js"]
  }
}
